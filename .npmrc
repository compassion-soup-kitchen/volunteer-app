# Expo + Metro resolve most reliably against a flat (npm-style) node_modules
# layout. pnpm's default symlinked/isolated layout trips up React Native's
# Haste module map in a monorepo, so we hoist for the whole workspace.
# This changes on-disk layout only — resolved versions still come from pnpm-lock.yaml.
node-linker=hoisted
